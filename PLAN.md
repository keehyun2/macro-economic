# 경상계 타임머신 — 한국은행 ECOS 기반 가계금융 부담·수혜 분석 사이트 계획서

> working name: `macro-economic-dashboard` (이후 확정)
> 데이터 출처: 한국은행 경제통계시스템 Open API (ECOS, https://ecos.bok.or.kr/api/)

## 1. 목표와 콘셉트

**"지금 같은 금융시장에서 누가 이득이고, 누가 손해를 보는가?"**

- 영끌족(고레버리지 주택구매자), 빚투족(차입금 투자자)처럼 부채로 자산을 보유한
  가계 페르소나의 **부담(이자비용)과 손익(자산가치 변동)을 지표로 계산**해 보여준다.
- 금리·물가·환율·주가라는 시장 변수의 현재 방향을 종합해, 페르소나별
  "이번 달 승자/패자"를 자동 판정한다.
- 개인별 정확한 재무 상담이 아니라, **공식 통계에 기반한 정보성 시각화**임을 명확히 한다.

### 핵심 질문 3가지 (사이트가 답해야 하는 것)

1. 지금 영끌/빚투의 이자 부담은 역사적으로 어느 수준인가? (백분위, 3년 전 대비)
2. 금리 사이클이 바뀌면 각 페르소나의 월 지출/자산이 얼마나 움직이는가?
3. 과거 금리 인상·인하 국면(예: 2021 인상기, 2024 인하기)에서 누가 이득/손실을 봤는가?

## 2. 페르소나 정의 (분석 단위)

| 페르소나 | 자산 | 부채 | 핵심 민감 변수 |
|---|---|---|---|
| 영끌족·변동금리 | 주택 | 주택담보대출(변동) | 주담대 금리↑ → 월이자 증가, 집값↓ → 자산 감소 |
| 영끌족·고정금리 | 주택 | 주택담보대출(고정) | 만기 재고정 시점의 금리 수준 |
| 빚투족 | 주식/코인 등 | 신용대출·마진논 | 자산수익률 − 차입금리 (레버리지 스프레드) |
| 예금족 | 예금·적금 | 없음 | 실질금리 = 예금금리 − CPI |
| 채권투자자 | 채권·채권펀드 | 없음 | 금리↓ → 평가손익 ↑ |
| 수입소비자/해외투자자 | — | — | 원/달러 환율 방향 |

- 페르소나별 민감도(부호·크기)는 코드 상수로 정의(`personas.ts`)하고,
  현재 시장 상태(각 지표의 3개월 변화 방향)와 곱해 **-100~+100 점수**를 낸다.
- v1은 6개 페르소나. 이후 확장(청년 무주택자, 자영업자 대출 등).

## 3. 데이터 소스 — ECOS 통계표 매핑

### 3.1 ECOS API 기본 구조 (구현 치트시트)

```
GET https://ecos.bok.or.kr/api/StatisticSearch/{인증키}/{json|xml}/kr/{시작건수}/{종료건수}/{통계표코드}/{주기 A|Q|M|D}/{검색시작일}/{검색종료일}/{통계항목코드}
```

- 주기별 검색일자 형식: 연간 `2023`, 분기 `2023Q1`, 월간 `202301`, 일간 `20230101`
- 응답 JSON: `StatisticSearch.result.code === "000"` 이어야 정상,
  `row[]`에 `TIME`(시점) / `DATA_VALUE`(값) / `ITEM_NAME1`(항목명) 배열
- 인증키는 홈페이지 회원가입 후 발급. **서버에서만 사용** (클라이언트 노출 금지)
- 항목코드·수록기간은 `StatisticItemList` 서비스로 사전 확인 가능

### 3.2 지표 → 통계표 매핑 (v1 대상)

| 지표 | ECOS 통계표 | 주기 | 상태 |
|---|---|---|---|
| 한국은행 기준금리 | `722Y001` (항목: 한국은행 기준금리) | M/D | 확정 (블로그 다수 검증) |
| 국고채 수익률(1/3/10년) | `722Y001` 항목별 | M | 확정 (항목코드 구현 시 확정) |
| 시장금리(일별, 콜금리 등) | `817Y002` | D | 확정 (항목코드 확인 필요) |
| 예금은행 대출금리(신규취급) — 주담대/신용대출 | `121Y006` | M | 확정 (2006~ 수록, 주담대·신용대출 금리 항목 포함) |
| 예금은행 예금금리(신규취급) — 정기예금 | `121Y007`(후보) | M | 확인 필요 |
| 원/달러 환율(매매기준율) | `731Y001`(후보) | D | 확인 필요 |
| 소비자물가지수(CPI) | `901Y006`(후보, 통계청 미러) | M | 확인 필요 |
| 가계신용(가계대출 잔액) | `403Y001`(후보, 연간 + 분기 표 별도) | Q | 확인 필요 |
| 예금은행 여신(대출잔액 증감) | `121Y001` 계열(후보) | M | 확인 필요 |
| 경제심리지수 | `200Y101` | M | 확정 |
| KOSPI | ECOS 수록 여부 확인, 없으면 KRX/외부 | D | **열림** (아래 참고) |

### 3.3 ECOS 밖에서 보완해야 할 데이터 (선택)

- **주택가격지수**: ECOS에 없으면 한국부동산원·KOSIS 오픈API 또는 KB 주택가격 동향.
  없으면 v1은 "주담대 금리 + 가계대출"로 영끌 부담만 다루고 집값은 v2로.
- **KOSPI**: ECOS에 주가지수가 수록되어 있지 않으면 KRX 정보데이터시스템(인증키 필요)
  또는 ECOS 내 대체지표로 처리. 빚투족 페이지의 필수 입력은 아니 — 신용대출 금리만으로
  "빚투 손익분기 수익률" (break-even return = 차입금리)은 계산 가능하다.
- **소득 대리지표**(부담 비율 계산용): 1인당 GDP 또는 임금 관련 ECOS 통계. 없으면
  부담은 "금리 수준 + 증감"으로만 표현(절대 소득 대비 비율은 v2).

### 3.4 Phase 0에서 할 일 (통계코드 확정)

1. ECOS 회원가입 → 인증키 발급 (환경변수 `ECOS_API_KEY`)
2. 개발가이드 → 통계코드검색 화면에서 위 표의 "확인 필요" 항목 확정
3. 항목코드는 `StatisticItemList`로 수록기간과 함께 검증, `src/lib/stat-codes.ts`에
   상수 + JSDoc으로 고정
4. 일일 호출 한도 확인 → 캐싱 정책(아래 6.3) 반영

## 4. 파생 지표 (계산 엔진)

모두 서버에서 계산해 JSON으로 내려준다. 공통 유틸: 월 단위 시계열 정렬/조인,
전월·전년동월 대비, 3개월 이동평균, 역사 백분위(2000년 이후), 롤링 12개월 수익률.

| 지표 | 계산 | 쓰이는 페이지 |
|---|---|---|
| 실질 기준금리 | 기준금리 − CPI 전년동월비 | 대시보드, 예금족 |
| 영끌 월이자(대표 케이스) | 대출 원금 P(기본 3억, 슬라이더) × 주담대 신규금리 / 12 | 영끌족 |
| 금리충격 환산 | Δ기준금리 1%p × P → 월 이자 증가액 | 영끌족 |
| 이자부담 백분위 | 현재 주담대 금리의 2006년 이후 백분위 | 영끌족 |
| 빚투 손익분기 수익률 | 신용대출(신규) 금리 = 이것보다 못 벌면 마이너스 | 빚투족 |
| 레버리지 스프레드 | KOSPI 12M 수익률 − 신용대출 금리 (KOSPI 확보 시) | 빚투족 |
| 예금 실질수익 | 정기예금 신규금리 − CPI 전년동월비 | 예금족 |
| 페르소나 점수 | Σ(시장변수 3M 방향 × 페르소나 민감도), -100~+100 정규화 | 승자/패자 |
| 사이클 리플레이 | 과거 금리 국면 구간 정의 → 구간별 페르소나별 손익 재현 | 매트릭스 |

주의: 지연 공표(월간 통계는 대부분 익월, 가계신용은 분기) → 모든 수치에
"데이터 시점(yyyy-mm)"을 함께 표시한다.

## 5. 페이지 구성

```
/                 대시보드
                  ├─ 핵심 지표 타일 5개(기준금리, CPI, 원/달러, 주담대금리, 경제심리)
                  ├─ "이번 달 승자/패자" 페르소나 카드 6장 (점수 + 기여 지표 태그)
                  └─ 기준금리 vs 주담대금리 vs CPI 멀티 라인 차트 (10년)

/youngkul         영끌족 — 이자 부담 시뮬레이터
                  ├─ 대출 원금 슬라이더(1억~10억) + 변동/고정 토글
                  ├─ 월 이자 추이 꺾은선 + "3년 전 대비 ±○○만원" 콜아웃
                  ├─ 현재 금리의 역사 백분위 게이지
                  └─ 기준금리 1%p 변동 시 민감도 표

/debt-invest      빚투족 — 레버리지 성과
                  ├─ 손익분기 수익률 추이 (신용대출 금리)
                  ├─ (KOSPI 확보 시) 레버리지 스프레드 히트맵: 시기별 성공/실패 구간
                  └─ 금리 상승기 빚투 실패 사례 하이라이트(2021→2022)

/matrix           승자·패자 매트릭스
                  ├─ 현재 시장 상태 오버레이(금리↓ 물가↓ 환율↑ …)
                  ├─ 시나리오(금리↑/↓ × 물가↑/↓ × 환율↑/↓) × 페르소나 히트맵
                  └─ 과거 사이클 리플레이: 국면 선택 → 그때의 승자/패자

/data             출처와 업데이트
                  └─ 통계표 목록, 데이터 시점, 출처 표기, 문의
```

모든 차트 하단에 "출처: 한국은행 경제통계시스템 (ECOS)" 표기. 모바일 반응형 필수.

## 6. 기술 아키텍처

### 6.1 스택 (권장)

- **Next.js 14+ (App Router, TypeScript)** — API 라우트가 ECOS 프록시 역할을 겸해
  프론트/백을 한 저장소에서 처리. 인증키는 서버 환경변수로만 존재.
- **차트**: Recharts (또는 ECharts — 히트맵·게이지가 많으면 ECharts 유리)
- **스타일**: Tailwind CSS
- **대안**: 데이터 전처리를 파이썬으로 하고 싶으면 FastAPI + React(Vite) 분리 구성도 가능.
  단 v1 규모에선 Next.js 단일 구성이 운영 비용이 가장 낮다.

### 6.2 디렉터리 (Next.js 구성 시)

```
macro-economic/
  src/
    lib/
      ecos.ts          # ECOS fetch 래퍼: result code 검증, 타임아웃, 재시도, 에러 정규화
      stat-codes.ts    # 통계표·항목코드 상수 (Phase 0 산출물의 단일 진실 원천)
      series.ts        # 시계열 유틸: 정렬/조인/YoY/백분위/롤링수익률
      indicators.ts    # 파생 지표 계산 (4장 표의 로직)
      personas.ts      # 페르소나 정의 + 민감도 + 점수 계산
    app/
      api/series/[key]/route.ts   # 캐시된 지표 시계열 JSON
      (페이지 라우트 — 5장 구성)
    components/        # KpiTile, PersonaCard, ScenarioHeatmap, InterestSimulator ...
```

### 6.3 캐싱 전략 (필수 — ECOS 일일 호출 제한 대응)

- API 라우트에 `revalidate`(ISR) 적용: 일간 지표 6시간, 월간 지표 24시간, 분기 지표 7일.
- ECOS는 하루 호출 수 제한이 있으므로 **같은 통계 요청은 반드시 캐시 hit**으로 처리.
- 개발 중에는 로컬 JSON 스냅샷(`data/snapshots/`)에 저장해 재호출 없이 작업.
- 응답에는 `dataAsOf`(데이터 시점)와 `fetchedAt`을 함께 실어 UI에 표시.

### 6.4 배포

- 1순위 Vercel (환경변수 `ECOS_API_KEY`, ISR 그대로 동작).
- 사내/자체 호스팅이면 Node 런타임 + Docker. ECOS가 국외 IP를 차단하는 경우가
  보고된 적 있으니 배포 리전은 서울(또는 국내 서버) 권장 — 이것도 Phase 0에서 1회 검증.

## 7. 마일스톤

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| M0. 통계코드 확정 | 인증키 발급, 3.2 표의 "확인 필요" 확정, `stat-codes.ts` 작성, 호출 한도·국외 차단 확인 | 주요 6개 지표가 로컬 스크립트로 정상 수신 |
| M1. 데이터 레이어 | `ecos.ts` + 캐싱 + `/api/series` + 스냅샷 저장 | 브라우저 없이 curl로 지표 시계열 JSON 반환 |
| M2. 지표 엔진 | `series.ts` + `indicators.ts` (실질금리, 월이자, 백분위, 손익분기 수익률) | 지표 단위 테스트(과거 알려진 값 교차 검증) |
| M3. 대시보드 | `/` 페이지: 지표 타일 + 멀티 라인 차트 | 모바일에서 깨지지 않는 대시보드 1장 |
| M4. 페르소나 3종 페이지 | `/youngkul`, `/debt-invest`, `/matrix` + `personas.ts` 점수 | 핵심 질문 3가지(1장)에 답하는 화면 완성 |
| M5. 배포·마무리 | 출처 표기, `/data` 페이지, 배포, README | 실 URL에서 동작 |

권장 순서는 M0→M5 직진. KOSPI·주택가격지수 같은 "열림" 항목은 M4에서
비어 있어도 페이지가 성립하도록 먼저 만들고, 확보되면 채워 넣는다.

## 8. 제약·리스크·준수사항

- **인증키 보안**: 클라이언트 번들·저장소에 절대 노출 금지. 서버 환경변수만.
- **호출 제한**: ECOS 서비스별 일일 한도. 캐싱 없이는 배포 즉시 막힘 (6.3 필수).
- **출처 표기**: "출처: 한국은행 경제통계시스템"을 모든 차트/표에 표기 (이용지침).
- **지연 공표**: 월간 익월, 가계신용 분기. 데이터 시점 명시로 오해 방지.
- **투자 권유 아님**: 페르소나 점수는 통계 기반 해석이지 상품 추천이 아님을 하단 고지.
- **외부 데이터 의존**: 주택가격/KOSPI는 ECOS 외부(부동산원/KRX)일 수 있음 —
  v1 범위를 ECOS만으로 먼저 닫고 외부 소스는 v2로 미루는 것이 리스크 최소.

## 9. v1 비-목표 / 확장 아이디어

**비-목표(v1)**: 로그인·개인 포트폴리오 입력, 실시간(분 단위) 시세, 주택 실거래가 조회,
AI 해석 문구, 다국어(한국어 우선).

**확장(v2+)**:
- 주택가격지수 결합 → 영끌족 "순자산" 추이 (LTV 가정 시뮬레이션)
- 개인 대출 조건 입력 → DSR 계산기
- 뉴스 이벤트(금통위 일정) 오버레이
- 과거 국면으로 "타임머신" — 2021년 말의 지표와 오늘을 나란히 비교
- 알림(기준금리 변경 시 페르소나별 영향 요약)
